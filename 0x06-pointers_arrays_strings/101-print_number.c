#include "main.h"

/**
 * print_number - prints a number
 *
 * @n: integer
 *
 * Return: nothing
 */

void print_number(int n)
{
	unsigned int i;

	if (n < 0)
	{
		_putchar('-');
		n = n * -1;
	}
	i = n;
	
	if (x / 10)
	{
		print_number(x / 10);
	}
	_putchar(x % 10 + '0');
}
