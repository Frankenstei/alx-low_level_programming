#include "main.h"

/**
 * _puts - writes str to stdout
 *
 * @str: a character
 *
 * Return: Always 0
 */

void _puts(char *str)
{
	while (s != '\0')
	{
		_putchar(s);
		s++;
	}
	_putchar('\n');
	return (0);
}
