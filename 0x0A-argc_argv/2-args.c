#include <stdio.h>

/**
 * main - prints arguments received
 *
 * @argc: argument count
 * @argv: argument vector
 *
 * Return: 0
 */

int main(argc, *argv[])
{
	int i;

	for (i =1; i < argc; i++)
	{
		printf("%i\n", argv[i]);
	}
	return (0);
}#include <stdio.h>

/**
 * main - prints arguments received
 *
 * @argc: argument count
 * @argv: argument vector
 *
 * Return: 0
 */

int main(argc, *argv[])
{
	int i;

	for (i =1; i < argc; i++)
	{
		printf("%i\n", argv[i]);
	}
	return (0);
}#include <stdio.h>

/**
 * main - prints arguments received
 *
 * @argc: argument count
 * @argv: argument vector
 *
 * Return: 0
 */

int main(argc, *argv[])
{
	int i;

	for (i =1; i < argc; i++)
	{
		printf("%i\n", argv[i]);
	}
	return (0);
}
