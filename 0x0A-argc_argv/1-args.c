#include <stdio.h>

/**
 * main - prints the number of argument
 *
 * @argc: argument count
 * @argv: argument vector
 *
 * Return: 0
 */

int main(argc, *argv[])
{
	printf("%i\n", argc);
	return (0);
}
